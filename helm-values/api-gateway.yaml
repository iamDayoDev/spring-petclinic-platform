image:
  tag: "4c0d043"

service:
  name: api-gateway
  port: 80
  targetPort: http

container:
  port: 8080

spring:
  profiles: docker

dependencies:
  configServer:
    enabled: true
  discoveryServer:
    enabled: false
