image:
  tag: "a99d4d6"

service:
  name: config-server
  port: 8888
  targetPort: http

container:
  port: 8888

spring:
  profiles: default

dependencies:
  configServer:
    enabled: false
  discoveryServer:
    enabled: false
