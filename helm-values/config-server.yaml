image:
  tag: "7ec6c3d"

service:
  name: config-server
  port: 8888
  targetPort: http

container:
  port: 8888

spring:
  profiles: default

dependencies:
  configServer:
    enabled: false
  discoveryServer:
    enabled: false
