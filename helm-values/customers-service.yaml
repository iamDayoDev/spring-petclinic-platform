image:
  tag: "7ec6c3d"

service:
  name: customers-service
  port: 8081
  targetPort: http

container:
  port: 8081

spring:
  profiles: docker,mysql

dependencies:
  configServer:
    enabled: true
  discoveryServer:
    enabled: true

mysql:
  enabled: true
